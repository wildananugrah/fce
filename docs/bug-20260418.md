1. AI Skills should be mapped per workspace level
2. AI Provider should be mapped per workspace level as well. 

The RBAC, need to think about it. 
    ROLES:
        1. SUPERADMIN 
            -> maintain the workspace and project
            -> maintain users
            -> assigned users to workspace and project
            -> Profile settings
        2. ADMIN
            -> assigned users to become a member of a project
            -> maintain users -> edit only details and password
            -> set member project as APPROVER or NOT (optional)
            -> mapping member in a project to some menus
            -> Profile settings
        3. MEMBER
            -> brand brain
            -> product brain
            -> topic generator
            -> campaign generator
            -> topic library
            -> AI Skills
            -> Learning Center
            -> Research Hub
            -> Profile settings

    MEMBER with APROVER attr can change status in
        -> topic in topic library page
        -> content in content library page